# Copy this file to .env and fill in real values. .env is gitignored.

# Example secret consumed by data_pipeline.resources.ExampleAPIResource
EXAMPLE_API_KEY=

# Postgres connection used by docker-compose for Dagster run/event storage
POSTGRES_USER=dagster
POSTGRES_PASSWORD=dagster
POSTGRES_DB=dagster
